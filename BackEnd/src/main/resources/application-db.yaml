#spring:
#  datasource:
#    # 도커 환경에 맞게 수정 (localhost:1521/서비스네임)
#    url: jdbc:oracle:thin:@localhost:1521/xe
#    username: smartfarm # 혹은 설정한 계정
#    password: password123 # 설정한 비밀번호
#    driver-class-name: oracle.jdbc.OracleDriver
#  jpa:
#    hibernate:
#      ddl-auto: update # 엔티티 기반으로 테이블 자동 생성
#    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.OracleDialect

spring:
  datasource:
    # 환경 변수가 없으면 localhost의 MySQL을 보게 하세
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/smartfarm}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update